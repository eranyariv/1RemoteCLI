# --- .NET ---
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo
*.userprefs
.vs/
TestResults/
*.trx
*.coverage
coverage.*.xml

# --- Node / PWA ---
node_modules/
dist/
dist-e2e/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# The phone app, staged into the hub at deploy time by scripts/publish-hub.ps1.
# It is build output that happens to live under src/, not source.
src/Hub/wwwroot/

# --- Playwright ---
playwright-report/
test-results/
.playwright/

# --- Editors ---
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# --- OS ---
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# --- Secrets and local config ---
# Never commit real endpoints, client secrets, or VAPID keys.
# Nor the deployment target itself: account name, tenant id, subscription id.
azure-target.local.md
*.env
.env.*
!.env.example
appsettings.Development.json
appsettings.Local.json
secrets.json
*.pfx
*.pem
*.key

# Azure CLI profile, in case anyone points AZURE_CONFIG_DIR inside the repo.
# It stores refresh tokens; see scripts/az-env.ps1 for the supported location.
.azure/
