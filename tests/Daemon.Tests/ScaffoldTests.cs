namespace OneRemoteCli.Daemon.Tests;

/// <summary>
/// Placeholder so the test project has at least one test until the real
/// suites land. Delete once Daemon has real coverage.
/// </summary>
public class ScaffoldTests
{
    [Fact]
    public void ProjectIsWiredUp()
    {
        Assert.True(true);
    }
}
